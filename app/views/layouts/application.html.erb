<!DOCTYPE html>
<html lang="zh">
  <head>
    <title><%= content_for(:title) || "BreakfastTracker" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= favicon_link_tag asset_path("fried-egg.webp") %>
    <%= favicon_link_tag asset_path("fried-egg.webp"), rel: "apple-touch-icon", type: "image/webp" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="mx-6 my-4 mx-auto">
      <%= render "shared/flash" %>
    </div>
    <main class="relative">
      <%= yield %>
    </main>
  </body>
</html>
